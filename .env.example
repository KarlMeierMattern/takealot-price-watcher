# Copy these values into GitHub repo secrets (Settings → Secrets → Actions).
# Local .env is only needed if you run `npm run check` on your machine.

RESEND_API_KEY=re_xxxxxxxx
EMAIL_FROM=Price Watcher <onboarding@resend.dev>
EMAIL_TO=you@example.com

# Optional secrets
# TARGET_PRICE=7500
# RETAILERS=takealot,amazon
